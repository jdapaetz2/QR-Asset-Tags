# Copy to .env.local and fill in. NEVER commit real secrets.
# Every variable below is actually read by lib/env.ts. See docs/CODE_HANDOFF.md
# "Environment variables". Storage bucket names are hard-coded in code (not
# configurable), and the public-form honeypot is a fixed internal field
# (`company_website`) — so neither is an environment variable.

# Supabase (public — safe in the browser bundle)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase service role (SERVER ONLY — never expose to the client)
SUPABASE_SERVICE_ROLE_KEY=

# Base URL used to build permanent QR URLs (the /t/{short_code} host).
# PRODUCTION-DOMAIN-SENSITIVE: physical tags encode this value permanently, so
# production must be the final stable host — never localhost or a *.vercel.app
# preview (blocked from tag production by isProductionBaseUrl).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Salt for hashing scan-event IPs AND public-intake rate-limit keys (server only;
# never store raw IPs). Fails CLOSED in Vercel production/preview (must be >= 32 chars);
# fail-soft only in local/test. Phase A4 reuses this salt for the shared-store rate
# limiter's IP/short-code hashes — no separate rate-limit salt is needed.
SCAN_IP_HASH_SALT=

# Notification email (server only). Leave RESEND_API_KEY / NOTIFICATION_FROM_EMAIL
# blank to run notifications in DRY-RUN mode: the notifier logs a structured
# `[notifications]` line with outcome="dry_run" and makes NO network request —
# never sent, never blocks a submission. Provider: Resend (REST API, no SDK).
#
# LIVE EMAIL IS PRODUCTION-ONLY. Vercel Preview/staging is refused in code
# (lib/notifications/send.ts returns dry_run with reason="preview_environment"
# before reading any credential), so adding a key to Preview cannot send mail.
# Do not add these to the Preview environment.
#
# Production values (Phase B4 — see docs/EMAIL_CONFIGURATION_CHECKLIST.md):
#   NOTIFICATION_FROM_EMAIL     Mulemark <notifications@notify.mulemark.io>
#   NOTIFICATION_REPLY_TO_EMAIL support@mulemark.io
# NOTIFICATION_FROM_EMAIL must be on the Resend-verified sending subdomain; a
# malformed value is reported as failed_configuration and no send is attempted.
# NOTIFICATION_REPLY_TO_EMAIL is optional and points replies at a HUMAN mailbox
# (Google Workspace) rather than the no-reply sending address.
RESEND_API_KEY=
NOTIFICATION_FROM_EMAIL=
NOTIFICATION_REPLY_TO_EMAIL=

# Public-submission rate limiting IS implemented (Phase A4): a shared Postgres
# fixed-window limiter (migration 0033 + lib/ratelimit). Thresholds are centralized
# code constants in lib/ratelimit/policy.ts — NOT env vars — so there is nothing to
# configure here. If your local .env.local still has SUBMISSION_RATE_LIMIT_* lines,
# they are dead config from before A4 and can be deleted.
